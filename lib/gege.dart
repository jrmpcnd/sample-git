import 'package:flutter/material.dart';

class Gege extends StatelessWidget {
  const Gege({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('data'),
    );
  }
}
